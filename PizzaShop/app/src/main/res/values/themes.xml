<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.PizzaShop" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/red</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="spinTheme">
        <item name="android:background">@drawable/ic_dropdown</item>
    </style>

    <style name="buttonStyle">
        <item name="android:textColor">@color/red</item>
        <item name="android:drawable">@drawable/button</item>
        <item name="android:background">@drawable/button</item>

    </style>

    <style name="MyTab" parent="Widget.Design.TabLayout">
        <item name="tabTextAppearance">@style/MyTextApper</item>
    </style>
    <style name="MyTextApper" parent="TextAppearance.Design.Tab">
        <item name="textAllCaps">false</item>
        <item name="android:textAllCaps">false</item>
    </style>
</resources>